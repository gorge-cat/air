# room_ac_turb.m 公式集

> 紧凑方程式格式，按 5 个功能模块组织。每条关键公式后附物理意义注释（# 开头）及符号说明表。代码行号对应 `room_ac_turb.m`。

---

## 全局符号表

| 符号 | 含义 | 单位 | 代码变量 |
|------|------|------|---------|
| ρ | 空气密度 | kg/m³ | `model.rho` (=1.2) |
| cₚ | 空气定压比热 | J/(kg·K) | `model.cp` (=1005) |
| T | 空气温度 | K | `T` |
| t | 时间 | s | — |
| u, w | x、z 方向速度 | m/s | `uFull`, `wFull` |
| Ω | 房间全域 | m³ | — |
| α_eff | 有效扩散系数 | m²/s | — |
| α_bg | 背景扩散系数 | m²/s | `model.alpha_bg` (=0.003) |
| α_jet | 射流附加扩散 | m²/s | `model.alpha_jet` (=0.04) |
| r_f | 风机转速比例 | — | `fanRatio` |
| W_j | 射流核心权重 | — | `jetWeight` |
| q̇_load | 单格热源率 | W | `heatRateField` |
| q̇_AC | 单格冷源率 | W | `coolingPower*coolingWeight` |
| Q̇_load | 全屋总热负荷 | W | `heatLoad` |
| Q̇_cool | 全屋总制冷 | W | `coolingPower` |
| Δx | 网格尺寸 | m | `dx` (=0.5) |
| Δt | 时间步长 | s | `dt` (=0.2) |
| C_cell | 单格热容 | J/K | `cellCapacity` (=150.75) |
| N | 每方向网格数 | — | `model.N` (=20) |
| L | 房间边长 | m | `model.L` (=10) |

---

## 1.1 能量控制方程

**偏微分形式：**

ρcₚ ∂T/∂t + ρcₚ(u ∂T/∂x + w ∂T/∂z) = ∇·(ρcₚ α_eff ∇T) + q̇_load − q̇_AC

# 全屋温度场由对流搬运、湍流扩散混合、热源加热、空调制冷四项平衡决定

| 符号 | 含义 |
|------|------|
| ρcₚ ∂T/∂t | 单位体积内能变化率（蓄热项） |
| ρcₚ(u ∂T/∂x + w ∂T/∂z) | 对流搬运的能量（对流项） |
| ∇·(ρcₚ α_eff ∇T) | 湍流扩散混合的能量（扩散项） |
| q̇_load | 热源加热功率密度（W/m³） |
| q̇_AC | 空调冷源功率密度（W/m³） |

**有效扩散系数：**

α_eff(x,t) = α_bg + r_f(t) α_jet W_j(x)

# 背景 α_bg=0.003 m²/s 全屋都有；射流区附加 α_jet=0.04 m²/s 是背景的 13 倍，且随风机转速 r_f 线性缩放
# 代码：:32-33, :142, :144-146, :206-207

| 符号 | 含义 |
|------|------|
| α_bg | 背景扩散系数，远离射流区空气的微弱搅动 (0.003 m²/s) |
| α_jet | 射流附加扩散系数，射流区剧烈搅动 (0.04 m²/s) |
| r_f(t) | 风机转速比例 [0.35, 1]，随压缩机比例变化 |
| W_j(x) | 射流核心权重 [0,1]，描述射流强弱的空间分布 |

**射流核心权重 W_j：**

W_j = exp[−r²/(2σ²)] · exp[−x/(0.8L)]

# 第一项高斯分布控制横截面形状（中心最强，往外指数衰减）；第二项轴向衰减（吹出 8m 强度剩 37%）
# 代码：:358-360

| 符号 | 含义 |
|------|------|
| r | 距射流中心线的径向距离 (m) |
| σ | 射流半宽 (m)，随 x 增大 |
| x | 沿射流前进方向距离 (m) |
| L | 房间边长 (10 m) |
| 0.8L | 射流衰减特征长度 (8 m) |

其中：

r² = (y − y_in)² + (z − z_c)²
σ(x) = σ_0 + k_s x        # σ_0=0.35m，k_s=0.10（每米半宽增加 0.1m）
z_c(x) = clip(z_in + x tanθ, 0, L)    # 撞顶/撞底后贴壁，verticalSlope=0

# 代码：:350-355

| 符号 | 含义 |
|------|------|
| y_in, z_in | 进风口 y、z 坐标（均=5m，房间中央） |
| z_c | 射流中心线高度 |
| σ_0 | 射流初始半宽 (0.35 m) |
| k_s | 射流扩展率 (0.10) |
| θ | 射流角度 (±30°/0°) |
| clip(·,0,L) | 限制在 [0, L] 范围内（撞墙贴壁） |

**冷量空间分配权重：**

w_cool = (0.002 + W_j) / ∑(0.002 + W_j)

# 0.002 是给远离射流角落留的背景冷量，归一化保证 ∑w_cool=1，使总冷量严格等于空调制冷功率
# 代码：:364-365

| 符号 | 含义 |
|------|------|
| w_cool | 单格冷量分配权重 |
| 0.002 | 角落背景冷量，避免远离射流区永远降不下来 |
| W_j | 射流核心权重 |

**积分守恒形式：**

d/dt ∫_Ω ρcₚ T dV = Q̇_load − Q̇_cool

# 对流与扩散离散满足全域能量守恒：∑ΔT_adv=0（投影去均值），∑ΔT_diff=0（通量一加一减），∑ΔT_buoy=0（一加一减）
# 全屋平均温度的变化仅由"热负荷−制冷量"决定，与冷量空间分布无关

| 符号 | 含义 |
|------|------|
| Ω | 房间全域 |
| ∫_Ω ρcₚ T dV | 全屋空气总内能 |
| Q̇_load | 全屋总热负荷 (W) |
| Q̇_cool | 全屋总制冷量 (W) |

**离散形式：**

Tⁿ⁺¹_ijk = Tⁿ_ijk + ΔT_src + ΔT_adv + ΔT_diff

# 代码：:208

| 符号 | 含义 |
|------|------|
| Tⁿ_ijk | 时刻 n 网格 (i,j,k) 的温度 |
| ΔT_src | 源项温度增量（热负荷+空调冷量） |
| ΔT_adv | 对流项温度增量 |
| ΔT_diff | 扩散项温度增量 |

ΔT_src = (Δt/C_cell)(q̇_ijk − Q̇_c · w_cool,ijk)

# 源项：热负荷加热 + 空调冷量提取，严格按 W→J→K 转换
# 代码：:203

| 符号 | 含义 |
|------|------|
| Δt | 时间步长 (0.2 s) |
| C_cell | 单格热容 (150.75 J/K) |
| q̇_ijk | 网格 (i,j,k) 的热源功率 (W) |
| Q̇_c | 空调实际制冷功率 (W) |
| w_cool,ijk | 网格 (i,j,k) 的冷量分配权重 |

C_cell = ρcₚ Δx³ = 1.2×1005×0.5³ = 150.75 J/K

# 让一个网格升温 1K 需 150.75 焦耳
# 代码：:90

---

## 1.2 显式空气对流

**对流方程：**

∂T/∂t = −u ∂T/∂x − w ∂T/∂z

# 温度被气流"搬运"到下游，自身不增不减
# 代码：:446 注释

| 符号 | 含义 |
|------|------|
| u, w | x、z 方向速度 |
| ∂T/∂x, ∂T/∂z | 温度梯度 |

**速度场（去均值保证体积守恒）：**

u_jet = v_0 W_j        # v_0=1.2 m/s
u = u_jet − ⟨u_jet⟩_{y,z}

# 减去 y-z 截面均值，模拟射流核心向前推 + 外围等量回流，封闭房间净流量为零
# 代码：:368-369

| 符号 | 含义 |
|------|------|
| u_jet | 射流核心 x 方向速度 |
| v_0 | 射流名义速度 (1.2 m/s) |
| W_j | 射流核心权重 |
| ⟨·⟩_{y,z} | 对 y-z 截面取平均 |
| u | 净 x 方向速度（去均值后） |

w_jet = u_jet · tanθ · 1[¬wall]    # wall: z_c* ≤ 0 ∨ z_c* ≥ L
w = w_jet − ⟨w_jet⟩_{y,z}

# 撞墙后 verticalSlope=0，转为水平贴壁射流
# 代码：:370-373

| 符号 | 含义 |
|------|------|
| w_jet | 射流核心 z 方向速度 |
| θ | 射流角度 |
| 1[¬wall] | 未撞墙指示函数（撞墙=0） |
| z_c* | 无约束射流中心高度 |

**速度分解（迎风需要）：**

u⁺ = max(u, 0)，u⁻ = min(u, 0)
w⁺ = max(w, 0)，w⁻ = min(w, 0)

# 把速度拆正负两部分，分别用上游格子离散
# 代码：:136-139

| 符号 | 含义 |
|------|------|
| u⁺, w⁺ | 速度的正部分（向右/向上流） |
| u⁻, w⁻ | 速度的负部分（向左/向下流） |

**迎风离散：**

ΔT_adv|_x = −(Δt/Δx)[u⁺(T_i − T_{i−1}) + u⁻(T_{i+1} − T_i)]

# u>0 用上游 T_{i−1}，u<0 用上游 T_{i+1}；一阶精度，截断误差等效数值扩散 α_num=uΔx/2
# 代码：:449-451

| 符号 | 含义 |
|------|------|
| Δt/Δx | 时间步与网格尺寸之比 |
| T_i, T_{i−1}, T_{i+1} | 当前格、左邻、右邻温度 |

ΔT_adv|_z 同理
# 代码：:453-455

ΔT_adv ← ΔT_adv − mean(ΔT_adv)

# 投影去均值：简化速度场不逐格无散，对流项可能产生虚假净加热/冷却，投影掉防止能量不守恒
# 代码：:459

**风机缩放：**

scale = (Δt/Δx) r_f

# 速度场按风机转速比例缩放，低风量时对流变弱
# 代码：:205

| 符号 | 含义 |
|------|------|
| scale | 速度缩放系数 |
| r_f | 风机转速比例 |

**CFL 稳定性：**

CFL = (Δt/Δx) max(|u|+|w|)
D = 6 α_max Δt / Δx²
S = CFL + D ≤ 0.95

# 显式格式要求一个时间步内信息传播不超过一个网格，组合数超 0.95 报错
# α_max = α_bg + α_jet = 0.043 m²/s
# 代码：:126-129

| 符号 | 含义 |
|------|------|
| CFL | Courant-Friedrichs-Lewy 数（对流稳定判据） |
| D | 扩散数 |
| S | 组合稳定数 |
| α_max | 最大扩散系数 (0.043 m²/s) |

---

## 1.3 真实热负荷

**围护结构传热：**

q̇_env,ijk = G_ijk (T_out − T_ijk)

# 外墙附近格子传热更多，温度差越大传热越快
# 代码：:385

| 符号 | 含义 |
|------|------|
| q̇_env,ijk | 网格 (i,j,k) 的围护传热率 (W) |
| G_ijk | 网格局部传热系数 (W/K) |
| T_out | 室外温度 |
| T_ijk | 网格温度 |

G_ijk = UA · n_face,ijk / ∑n_face

# UA=80 W/K 按贴外墙面数分配：角格 n_face=3、棱=2、面=1、内=0，保证 ∑G=UA
# 代码：:333

| 符号 | 含义 |
|------|------|
| UA | 围护总传热系数 (80 W/K) |
| n_face,ijk | 网格贴外面数 (0~3) |
| ∑n_face | 全屋贴面总数 |

**室外温度（日变化）：**

T_out(t) = T̄_out + A_out sin(2πt/86400 + φ_out)

# T̄_out=305.15K(32°C)，A_out=2K，φ_out=π/6，模拟一天 ±2°C 波动
# 代码：:379-380

| 符号 | 含义 |
|------|------|
| T̄_out | 室外平均温度 (305.15 K = 32°C) |
| A_out | 日变化幅值 (2 K) |
| 2π/86400 | 一天的角频率 (rad/s) |
| φ_out | 相位 (π/6，决定几点最热) |

**太阳辐射：**

q̇_sol(t) = q_sol · max(0, 0.70 + 0.30 sin(2πt/86400 + π/3))

# q_sol=500W，在 350~500W 间波动，相位与室外温度错开
# 代码：:381

| 符号 | 含义 |
|------|------|
| q̇_sol(t) | 当前时刻太阳辐射功率 (W) |
| q_sol | 太阳辐射峰值 (500 W) |
| 0.70 + 0.30 sin(...) | 太阳辐射比例 (0.4~1.0) |
| π/3 | 太阳相位（与室外温度相位错开） |

空间分布：w_sol = (1[x=x_N] + 0.5·1[z=z_N]) / ∑(·)

# 东墙权重 1，顶棚权重 0.5
# 代码：:339-340

| 符号 | 含义 |
|------|------|
| w_sol | 太阳辐射空间权重 |
| 1[x=x_N] | 东墙指示函数 |
| 1[z=z_N] | 顶棚指示函数 |
| 0.5 | 顶棚权重系数（顶棚只有一半得热效率） |

**内部热源：**

q̇_int = N_p q_p + q_eq + q_lit = 4×75 + 400 + 200 = 900 W

# 人员显热 + 设备 + 照明，只加在 z≤2m 人员活动区
# 代码：:382-383, :336-337

| 符号 | 含义 |
|------|------|
| q̇_int | 内部热源总功率 (900 W) |
| N_p | 人数 (4) |
| q_p | 每人显热 (75 W) |
| q_eq | 设备热负荷 (400 W) |
| q_lit | 照明热负荷 (200 W) |

**单格总热源：**

q̇_ijk = q̇_env,ijk + q̇_int · w_int,ijk + q̇_sol · w_sol,ijk

# 代码：:385-386

| 符号 | 含义 |
|------|------|
| w_int,ijk | 内部热源空间权重（z≤2m 活动区均匀） |
| w_sol,ijk | 太阳辐射空间权重 |

**全屋总热负荷：**

Q̇_load = ∑ q̇_ijk

# 代码：:387

---

## 1.4 连续变频模型

**定频机：**

r = 1

# 永远满载，达温后只能停启循环
# 代码：:393

| 符号 | 含义 |
|------|------|
| r | 压缩机运行比例 [0, 1] |

**前馈（预判）：**

r_ff = clip(Q̇_load / Q_nom, 0, 1)

# 按热负荷比例预判出力，反应快；Q_nom=3500W
# 代码：:397

| 符号 | 含义 |
|------|------|
| r_ff | 前馈项 |
| Q̇_load | 当前热负荷 (W) |
| Q_nom | 额定制冷量 (3500 W) |
| clip(·,0,1) | 限制在 [0,1] 范围 |

**误差信号：**

e = T_ret − T_set

# 回风温度偏离目标，正表示偏热
# 代码：:196 传入

| 符号 | 含义 |
|------|------|
| e | 温度误差 |
| T_ret | 回风温度（全屋平均温度近似） |
| T_set | 目标温度 (298.15 K = 25°C) |

**条件积分（|e| ≤ 2.5K 时）：**

I* = clip(I + K_i e Δt, −0.5, 0.5)

# K_i=4×10⁻⁵ 1/(K·s)，慢慢消除稳态误差；|e|>2.5K 时冻结积分防超调
# 代码：:398-403

| 符号 | 含义 |
|------|------|
| I | 积分项状态 |
| I* | 积分候选值 |
| K_i | 积分系数 (4×10⁻⁵ 1/(K·s)) |
| e | 温度误差 |
| ±0.5 | 积分限幅 |

**抗饱和：**

r_raw = r_ff + K_p e + I*

# 若 (r_raw > 1 ∧ e > 0) ∨ (r_raw < 0 ∧ e < 0) → 暂停积分
# 已饱和且误差继续推向饱和方向时别再积了
# 代码：:404-410

| 符号 | 含义 |
|------|------|
| r_raw | 未限幅的原始输出 |
| K_p | 比例系数 (0.08 1/K) |
| ∧, ∨ | 逻辑与、逻辑或 |

**输出：**

r = clip(r_ff + K_p e + I, 0, 1)

# K_p=0.08 1/K，温度每偏离 1K 出力调整 8%
# 代码：:411

**最小比例保护（0 < r < 0.20 时）：**

r = 0.20 if (e > −0.25K) ∨ (r_ff > 0.10)；否则 r = 0

# 避免在低效区间憋着烧电，要么至少 20% 运行，要么停机
# 代码：:413-419

| 符号 | 含义 |
|------|------|
| 0.20 | 压缩机最小运行比例 |
| −0.25K | 误差阈值 |
| 0.10 | 前馈阈值 |

**空调性能：**

r_f = 0.35 + 0.65 √r

# 风机转速比例，用 √r 是风量-功率折中
# 代码：:424

| 符号 | 含义 |
|------|------|
| r_f | 风机转速比例 [0.35, 1] |
| 0.35 | 风机最小比例 |
| 0.65 | 风机可调比例 |

ṁ = ṁ_nom · r_f        # ṁ_nom=0.2 kg/s，风量随转速线性变化
# 代码：:425

| 符号 | 含义 |
|------|------|
| ṁ | 实际质量流量 (kg/s) |
| ṁ_nom | 额定质量流量 (0.2 kg/s) |

P_fan = 15 + 65 r_f³ W

# 风机相似律 P∝n³，最低 15W、满载 80W
# 代码：:426

| 符号 | 含义 |
|------|------|
| P_fan | 风机功率 (W) |
| 15 | 风机最小功率 |
| 65 | 风机可调功率 (80−15) |
| r_f³ | 转速的三次方（相似律） |

Q̇_req = r · Q_nom

# 压缩机请求制冷量
# 代码：:436

| 符号 | 含义 |
|------|------|
| Q̇_req | 压缩机请求制冷量 (W) |

Q̇_air = ṁ c_p max(0, T_ret − T_sup,min)

# 风侧极限：风量×比热×最大温差，T_sup,min=287.15K(14°C)
# 物理意义：即使压缩机能力够，风量太小或温差太大也搬不走更多冷量
# 代码：:437

| 符号 | 含义 |
|------|------|
| Q̇_air | 风侧制冷极限 (W) |
| c_p | 空气比热 (1005 J/(kg·K)) |
| T_sup,min | 最低送风温度 (287.15 K = 14°C) |

Q̇_c = min(Q̇_req, Q̇_air)

# 实际制冷取两者较小值
# 代码：:438

| 符号 | 含义 |
|------|------|
| Q̇_c | 实际制冷功率 (W) |

T_sup = T_ret − Q̇_c / (ṁ c_p)

# 送风温度，由制冷量和风量反推
# 代码：:439

| 符号 | 含义 |
|------|------|
| T_sup | 送风温度 |

COP = 3.2 + 0.5(1 − r)

# 低负载 COP 更高（变频省电关键），满载 3.2，停机附近 3.7
# 代码：:440-441

| 符号 | 含义 |
|------|------|
| COP | 性能系数 |
| 3.2 | 满载 COP |
| 0.5 | 部分负载 COP 增益 |

P_el = Q̇_c / COP + P_fan

# 总电功率 = 压缩机耗电 + 风机耗电
# 代码：:442

| 符号 | 含义 |
|------|------|
| P_el | 总电功率 (W) |
| Q̇_c/COP | 压缩机耗电 |
| P_fan | 风机耗电 |

---

## 1.5 浮力混合与数值实现类

**浮力搅拌（每 1 秒做一次）：**

ΔT_k = T_{k+1} − T_k

# 相邻高度层温差，上层减下层
# 代码：:481

| 符号 | 含义 |
|------|------|
| ΔT_k | 第 k 层与 k+1 层的垂直温差 |
| T_k, T_{k+1} | 下层、上层温度 |

不稳定（ΔT < −0.3K，上冷下热，要翻腾）：

Ri = |ΔT| / 2
μ = min(0.15, 0.05(1 + Ri))

# 越不稳定混合越强，上限 0.15
# 代码：:484-486

| 符号 | 含义 |
|------|------|
| Ri | 简化 Richardson 数 |
| μ | 混合系数 |
| 0.15 | 混合系数上限 |
| −0.3K | 不稳定判据阈值 |

稳定（ΔT > 0.3K，上热下冷，分层）：

Ri = ΔT / 2
μ = max(0.005, 0.03 / (1 + Ri))

# 越稳定混合越弱，下限 0.005
# 代码：:488-490

# Richardson 数本应含剪切项，本模型简化为只用温差近似 Ri≈|ΔT|/2

| 符号 | 含义 |
|------|------|
| 0.005 | 混合系数下限 |
| 0.3K | 稳定判据阈值 |

**界面交换：**

E = 0.5 μ ΔT
T_k ← T_k + E
T_{k+1} ← T_{k+1} − E

# 一加一减，能量严格守恒
# 代码：:492-495

| 符号 | 含义 |
|------|------|
| E | 界面交换量 |

**扩散守恒离散：**

α_jet^face_{i+1/2} = 0.5(α_jet,ijk^field + α_jet,i+1,j,k^field)

# 面心扩散系数取相邻两格平均
# 代码：:144

| 符号 | 含义 |
|------|------|
| α_jet^face | 面心射流扩散系数 |
| α_jet^field | 网格中心射流扩散系数 |

F_{i+1/2} = (α_bg + r_f · α_jet^face_{i+1/2})(T_{i+1} − T_i)

# 通量 = 扩散系数 × 温差，背景扩散全屋有，射流扩散随风机转速缩放
# 代码：:466

| 符号 | 含义 |
|------|------|
| F_{i+1/2} | i 与 i+1 网格之间的扩散通量 |

ΔT_diff|_i = F_{i−1/2} − F_{i+1/2}

# 每个面通量从一侧加、另一侧减，∑ΔT_diff=0 严格守恒
# 代码：:467-468

| 符号 | 含义 |
|------|------|
| F_{i−1/2} | 左邻面进入的通量 |
| F_{i+1/2} | 右邻面流出的通量 |

**累计耗电：**

E_el(t) = E_el(t − Δt) + P_el · Δt

# 显式 Euler 积分电功率
# 代码：:200

| 符号 | 含义 |
|------|------|
| E_el | 累计耗电量 |

**统计量：**

T̄ = (1/N³) ∑ T_ijk        # 全屋平均温度
σ_T = std(T_ijk)            # 温度标准差，越小越均匀
ΔT_range = max T − min T    # 温度极差

# 代码：:175-177

| 符号 | 含义 |
|------|------|
| T̄ | 全屋平均温度 |
| σ_T | 温度标准差 |
| ΔT_range | 温度极差 |

|v|_max = max √(u² + w²)    # 最大速度，用于诊断

# 代码：:266

| 符号 | 含义 |
|------|------|
| |v|_max | 最大速度模 |

**发散检查：**

若 ∃ (i,j,k): T_ijk ∉ ℝ finite → error

# 数值发散的最后一道防线，温度变 NaN/Inf 立刻报错
# 代码：:214-217

| 符号 | 含义 |
|------|------|
| ∃ | 存在（存在量词） |
| ℝ finite | 有限实数 |

**时间步进节奏：**

每 0.2 秒：热负荷 → 控制器 → 空调性能 → 源项 → 对流 → 扩散 → 温度更新
每 1 秒（5 步）：浮力搅拌一次
每 30 秒：记录采样快照
每 ~36 分钟：打印进度
满 6 小时：结束，绘图保存

# 代码：:188-243 主循环

---

*公式集完。共 5 节，覆盖能量方程、对流、热负荷、变频控制、浮力混合与数值实现。所有符号含中文释义。*
